{
  "name": "work-calendar-helper",
  "version": "0.7.0",
  "private": true,
  "description": "업체별 업무 일정 · D-day 도우미 — 프론트엔드 검증/빌드 도구",
  "type": "module",
  "scripts": {
    "check": "node --check frontend/zip.js && node --check frontend/combobox.js && node --check frontend/core.js && node --check frontend/app.js",
    "build:standalone": "node tools/build-standalone.mjs",
    "test": "node tests/smoke.mjs",
    "test:e2e": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "playwright": "^1.56.1"
  }
}
