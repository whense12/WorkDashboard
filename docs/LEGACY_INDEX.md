# Legacy Index

이 프로젝트는 빈 `main` 에서 다시 시작했다(clean reboot).

아래 브랜치는 **보존 기록**이다.
**새 프로젝트의 권위 있는 요구사항이 아니며, 필요할 때만 참고한다.**
여기서 어떤 내용도 자동으로 승계되지 않는다. 참고하려면 그때마다 명시적으로 결정한다.

| 브랜치 | 설명 |
|---|---|
| `claude/practical-dijkstra-s7zstq` | 커밋 6개, 최종 2026-09-13. React 기반 프로토타입과 설계 문서가 들어 있다. |
| `claude/schedule-dashboard-spec-mockup-zo4ymd` | 커밋 53개, 최종 2026-09-04. Tauri 2 + 바닐라 JS 로 만든 실행 가능한 데스크톱 앱(v0.7.0)과 Windows CI 가 들어 있다. |
| `orchestration/bootstrap` | 위 브랜치에서 갈라져 나와 통제 계층 문서를 추가한 브랜치. 제품 코드는 변경하지 않았다. |
| `main` | 초기 커밋 1개뿐. 비어 있다. 이 브랜치의 출발점이다. |

위 두 구현 브랜치는 초기 커밋 외에 공유하는 코드가 없다. 서로 독립된 시도다.

이 문서는 목록일 뿐이다. 각 브랜치의 요구사항·결정·기능 목록은 여기에 옮겨 적지 않는다.
