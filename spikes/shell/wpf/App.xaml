<Application x:Class="ShellSpike.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>

        <!-- Neutral verification palette. Not product design. -->
        <Color x:Key="SurfaceColor">#1E1E1E</Color>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#1E1E1E" Opacity="0.85" />
        <SolidColorBrush x:Key="BorderBrush" Color="#3C3C3C" />
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#E6E6E6" />
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#9A9A9A" />

        <!-- System default font. No web fonts, no bundled fonts. -->
        <FontFamily x:Key="UiFont">Segoe UI</FontFamily>

        <Style x:Key="LineText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="Margin" Value="0,0,0,3" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display" />
        </Style>

        <Style x:Key="LineTextSecondary" TargetType="TextBlock" BasedOn="{StaticResource LineText}">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        </Style>

        <!-- Minimal toggle chrome, identical to the Tauri rule set: transparent background,
             1px #3C3C3C border; checked -> border #E6E6E6 and text #E6E6E6; unchecked -> text
             #9A9A9A. No fill colour is introduced, so only the four palette values are used. -->
        <Style x:Key="SpikeToggle" TargetType="ToggleButton">
            <Setter Property="FontFamily" Value="{StaticResource UiFont}" />
            <!-- 13, same as the body text, so the two toggles match the Tauri buttons,
                 which take `font: inherit` from the 13px surface. -->
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
            <Setter Property="HorizontalAlignment" Value="Left" />
            <Setter Property="Margin" Value="0,0,0,4" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Chrome"
                                Background="Transparent"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1"
                                Padding="8,3">
                            <ContentPresenter x:Name="Label"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Chrome" Property="BorderBrush"
                                        Value="{StaticResource TextPrimaryBrush}" />
                                <Setter TargetName="Label" Property="TextElement.Foreground"
                                        Value="{StaticResource TextPrimaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="False">
                                <Setter TargetName="Chrome" Property="BorderBrush"
                                        Value="{StaticResource BorderBrush}" />
                                <Setter TargetName="Label" Property="TextElement.Foreground"
                                        Value="{StaticResource TextSecondaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Application.Resources>
</Application>
