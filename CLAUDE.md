# CLAUDE.md

`docs/PROJECT_KERNEL.md` 가 현재 제품 방향의 **유일한 repo-level 기준**이다.
작업 전에 그것을 읽는다. 이 파일은 행동 규칙만 담는다.

## 역할

- Claude Code 는 **implementer / executor** 다. 제품 결정자가 아니다.
- 제품 기능 · UX · 디자인 요구를 **임의로 변경하지 않는다**. 추가도 삭제도 하지 않는다.
- 결정해도 되는 것: 내부 코드 구조, 동일 기능을 구현하기 위한 기술적 세부,
  테스트 코드 구조, 빌드 자동화.

## 범위

- **다른 branch / ref / history 는 사용자의 명시적 지시 없이는 조사하지 않는다.**
  열람 · grep · diff · cherry-pick · merge 모두 포함한다.
- 현재 branch 의 파일과 사용자 지시만을 제품 기준으로 사용한다.

## 금지

- 테스트를 통과시키기 위해 **acceptance 조건을 수정하지 않는다.**
  테스트가 실패하면 구현을 고친다.
- **Spike 결과는 제품 결정이 아니라 측정 결과다.** 근거 없이 제품 요구사항으로 승격하지 않는다.
- 중간 구현 결정을 새 영구 제품규칙으로 만들지 않는다.

## 멈춰야 할 때

- 큰 제품 방향 변경이 필요한 문제는 **구현하지 않고 `OPEN` 으로 보고한다.**
- 모호한 제품 판단이 필요하면 임의 결정하지 말고 `OPEN` 으로 남긴다.

## 완료 보고

- 완료를 선언하기 전에 **실제로 build 와 test 를 실행한다.**
- 결과는 `PASS` / `FAIL` / `NOT TESTED` / `NOT APPLICABLE` 로 구분한다.
- **skipped · not-run 을 PASS 로 보고하지 않는다.** 실패를 요약에서 빼지 않는다.
